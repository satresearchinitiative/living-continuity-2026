import React from 'react';

export function BuiltEnvironment(props) {
  // Placeholder component - no 3D model available
  return <group {...props} dispose={null} />;
}

export default BuiltEnvironment;
