import React from 'react';

export function UrbanInformality(props) {
  // Placeholder component - no 3D model available
  return <group {...props} dispose={null} />;
}

export default UrbanInformality;
